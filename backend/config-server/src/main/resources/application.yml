server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        native:
          search-locations: file:///F:/projects/flipkart-clone/backend/config-repo
           #search-locations: file:/config-repo
  profiles:
    active: native

management:
  endpoints:
    web:
      exposure:
        include: "*"

encrypt:
  key: my-secret-key-123

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka