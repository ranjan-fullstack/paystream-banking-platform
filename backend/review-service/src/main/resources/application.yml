server:
  port: 9009   # change for each service

spring:
  application:
    name: review-service

  config:
    import: optional:configserver:http://localhost:8888

eureka:
  client:

    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
