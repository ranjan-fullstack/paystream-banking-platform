apiVersion: v1
kind: Secret
metadata:
  name: product-service-secret
  namespace: flipkart
type: Opaque
# Values must be base64-encoded: echo -n "value" | base64
# DO NOT commit real credentials — inject via CI/CD or Vault
stringData:
  DB_PASSWORD: "change-me"
  JWT_SECRET: "change-me"
  AWS_ACCESS_KEY: "change-me"
  AWS_SECRET_KEY: "change-me"
  AWS_CLOUDFRONT_DOMAIN: "change-me.cloudfront.net"
